/* import { Test, TestingModule } from '@nestjs/testing';
import { FixedRateCdtService } from './fixed_rate_cdt.service'; */

describe('FixedRateCdtService', () => {
  /*   let service: FixedRateCdtService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [FixedRateCdtService],
    }).compile();

    service = module.get<FixedRateCdtService>(FixedRateCdtService);
  }); */

  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
